# Historical implementation plans

Files in this directory are retained as historical implementation notes. They may mention superseded workflows such as proposed/accepted statuses, accept/reject, supersede/replace, hard/soft enforcement, delete/remove, or primary UI item keys.

The authoritative current product contract is in the root `README.md`: active vs archived only, archive-only cleanup semantics, no routine delete/remove/supersede/replace workflows, and primary UI item keys hidden by default.
